package org.example;

import java.io.*;
import java.net.*;

public interface TryClose {
    public static void tryClose(Socket socket, BufferedReader bufferedReader, BufferedWriter bufferedWriter) {

    }
}
